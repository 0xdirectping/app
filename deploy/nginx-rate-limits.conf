# Add these lines to /etc/nginx/nginx.conf inside the http { } block
# Rate limit zones for API endpoints

limit_req_zone $binary_remote_addr zone=api_read:10m rate=60r/m;
limit_req_zone $binary_remote_addr zone=api_write:10m rate=10r/m;
limit_req_zone $binary_remote_addr zone=mcp:10m rate=30r/m;
