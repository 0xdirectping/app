# Install to /etc/fail2ban/jail.d/nginx-ratelimit.conf
[nginx-ratelimit]
enabled  = true
filter   = nginx-ratelimit
action   = iptables-multiport[name=nginx-ratelimit, port="http,https"]
logpath  = /var/log/nginx/error.log
findtime = 300
maxretry = 10
bantime  = 3600
